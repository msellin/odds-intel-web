// Client-safe constants shared by the Overview loader (server) and its charts (client).
/** The one chart series that holds every bot retired since the window began. */
export const RETIRED_SERIES = "retired";
